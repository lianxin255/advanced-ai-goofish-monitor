# 紧急

# 非紧急

- [x] 任务管理编辑页面可以设置该任务是否会进行通知推送。默认开启通知推送（真正有效还需开启系统设置中的通知推送）。
- [ ] 优化运行日志的显示。每行日志都要确保显示时间戳。每个日志划分好等级，是INFO 还是 WARN 还是ERROR。如果程序运行出现错误，要及时进行通知推送
  - 已完成：集中日志入口 `log_time`/`safe_print` 增加时间戳与等级（INFO/WARN/ERROR）；风控/验证/登录失效/失败重试等关键错误已改为 `log_error`/`log_warn`；任务运行出错仍会调用 `_notify_task_failure` 推送通知。
  - 待办：仍有部分普通 `print` 语句未带时间戳，需要统一收敛为集中日志入口（量大，分阶段处理）。
